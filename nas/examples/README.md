# `workshop/nas/examples` — fixtures didáticos

Pastas numeradas (`01_scripts` … `09_final_video`) espelham a estrutura típica de um job no NAS de produção, para **experimentos** (video compose, publicação, TTS batch) sem misturar com saídas reais.

Código que resolve paths: [`workshop/publish_experiments/paths.py`](../../publish_experiments/paths.py), [`workshop/video_compose_experiments/paths.py`](../../video_compose_experiments/paths.py).

Saídas efémeras do pipeline → [`../jobs/README.md`](../jobs/README.md).
